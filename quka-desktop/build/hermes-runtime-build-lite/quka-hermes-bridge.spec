# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['../../hermes-runtime/quka_hermes_bridge.py'],
    pathex=[],
    binaries=[],
    datas=[],
    hiddenimports=['run_agent', 'model_tools', 'toolsets'],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    [],
    exclude_binaries=True,
    name='quka-hermes-bridge',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    console=True,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)
coll = COLLECT(
    exe,
    a.binaries,
    a.datas,
    strip=False,
    upx=True,
    upx_exclude=[],
    name='quka-hermes-bridge',
)
